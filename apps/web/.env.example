# SafeJourney agent-api base URL
VITE_API_URL=http://localhost:8080

# Optional: a MapTiler / Google style key for prettier maps. If unset, a keyless OSM
# raster style is used so the app renders with zero setup.
VITE_MAP_STYLE_URL=
